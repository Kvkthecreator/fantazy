# =============================================================================
# FANTAZY - Environment Configuration
# =============================================================================
# Copy this file to .env.local (frontend) or .env (backend) and fill in values
# Cozy Companion Backend - AI characters that remember your story

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development

# -----------------------------------------------------------------------------
# SUPABASE - Core Configuration
# -----------------------------------------------------------------------------
# Project URL (public)
NEXT_PUBLIC_SUPABASE_URL=https://lfwhdzwbikyzalpbwfnd.supabase.co
SUPABASE_URL=https://lfwhdzwbikyzalpbwfnd.supabase.co

# Anon/Public Key (safe for frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (backend only - NEVER expose to frontend)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# DATABASE - Connection
# -----------------------------------------------------------------------------
# Session Pooler (IPv4 compatible - recommended for Render)
# Format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql://postgres.lfwhdzwbikyzalpbwfnd:your-password@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres

# For local scripts (dump_schema.sh):
FANTAZY_DB_PASSWORD=your-password

# -----------------------------------------------------------------------------
# JWT - Authentication
# -----------------------------------------------------------------------------
SUPABASE_JWT_SECRET=your-jwt-secret

# JWT verification URLs
SUPABASE_JWKS_URL=https://lfwhdzwbikyzalpbwfnd.supabase.co/auth/v1/keys
SUPABASE_JWKS_ISSUER=https://lfwhdzwbikyzalpbwfnd.supabase.co/auth/v1
SUPABASE_JWT_AUD=authenticated

# -----------------------------------------------------------------------------
# API - Backend Configuration
# -----------------------------------------------------------------------------
API_ENV=development

# CORS origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# FRONTEND - Site Configuration
# -----------------------------------------------------------------------------
# Site URL (for absolute URLs, OAuth callbacks)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# API URL (if using separate backend)
NEXT_PUBLIC_API_URL=http://localhost:10000

# -----------------------------------------------------------------------------
# AI - OpenAI for character conversations
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-key
